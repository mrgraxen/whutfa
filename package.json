{
  "name": "whutfa",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "license": "GPL-3.0-or-later",
  "description": "WHUTFA — Web Head Unit Transformator For Android",
  "scripts": {
    "build": "npm run build --workspace=server",
    "test": "npm run test --workspace=server && node --test frontend/*.test.js",
    "test:integration": "npm run test:integration --workspace=server",
    "fixtures": "node scripts/generate-fixtures.js",
    "setup:host-usb": "bash scripts/install-host-usb.sh"
  },
  "workspaces": ["server"]
}
